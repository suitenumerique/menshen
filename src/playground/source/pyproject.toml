[project]
name = "source"
version = "0.1.0"
description = "Token exchange playground source service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.3",
    "django-lasuite>=0.0.25",
]
